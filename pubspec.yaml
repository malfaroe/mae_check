name: mae_check
description: App de checklists personales

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.3.0
  uuid: ^4.4.0
  google_fonts: ^6.2.1
  share_plus: ^10.0.3
  path_provider: ^2.1.3
  file_picker: ^8.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
